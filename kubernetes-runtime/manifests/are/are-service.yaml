apiVersion: v1
kind: Service
metadata:
  name: are-service
  labels:
    app: are
    app.kubernetes.io/component: runtime
    app.kubernetes.io/part-of: aigenzey-runtime
spec:
  type: ClusterIP
  selector:
    app: are
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
