apiVersion: v1
kind: Service
metadata:
  name: crawl4ai-service
  labels:
    app: crawl4ai
    app.kubernetes.io/component: scraper
    app.kubernetes.io/part-of: aigenzey-runtime
spec:
  type: ClusterIP
  selector:
    app: crawl4ai
  ports:
  - name: http
    port: 11235
    targetPort: 11235
    protocol: TCP
